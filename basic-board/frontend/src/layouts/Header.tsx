// @flow
import * as React from 'react';
import { Layout } from 'antd';

type Props = {};
export const Header = (props: Props) => {
  return <Layout.Header className="blue">Header</Layout.Header>;
};
